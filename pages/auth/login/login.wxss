.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: #f9fafb;
  padding: 60rpx;
  box-sizing: border-box;
}

/* Logo */
.logo-box {
  text-align: center;
  margin-bottom: 100rpx;
}
.logo {
  width: 160rpx;
  height: 160rpx;
  margin-bottom: 20rpx;
}
.title {
  font-size: 42rpx;
  font-weight: bold;
  color: #111827;
}

.input-box {
  position: relative;            /* 供右侧图标定位 */
  border: 1px solid #D9D9D9;     /* 边框色 */
  border-radius: 12rpx;          /* 圆角 */
  padding: 12rpx 24rpx;          /* 内边距 */
  margin: 24rpx 0;               /* 上下间距 */
  background-color: #fff;        /* 背景 */
  box-sizing: border-box;
}

/* 🟢 输入框本体（去自带边框，使用外层） */
.login-input {
  width: 100%;
  height: 40rpx;
  border: none;
  outline: none;
  font-size: 30rpx;
  color: #333;
  padding-right: 76rpx;          /* 给右侧图标留空间 */
  box-sizing: border-box;
  background: transparent;
}

/* 🟢 特定：有图标的容器再多留点空间 */
.has-icon .login-input { padding-right: 88rpx; }

/* 🟢 小眼睛图标 */
.eye-icon {
  position: absolute;
  right: 20rpx;
  top: 50%;
  width: 44rpx;
  height: 44rpx;
  transform: translateY(-50%);
  opacity: 0.82;
}
.eye-icon:active { opacity: 1; }

/* 按钮 */
.btn-group {
  width: 100%;
  max-width: 600rpx;
}
button {
  width: 100%;
  margin: 25rpx 0;
  padding: 26rpx;
  border-radius: 50rpx;
  font-size: 32rpx;
  font-weight: bold;
  text-align: center;
  border: none;
}

/* 主按钮 */
.btn-primary {
  background: linear-gradient(90deg, #6366f1, #4f46e5);
  color: white;
  box-shadow: 0 6rpx 12rpx rgba(79,70,229,0.3);
}

/* 微信按钮 */
.btn-wechat {
  background: #1AAD19;
  color: white;
  box-shadow: 0 6rpx 12rpx rgba(26,173,25,0.3);
}

/* 底部提示 */
.footer {
  margin-top: 80rpx;
  font-size: 28rpx;
  color: #6b7280;
}
.link {
  color: #2563eb;
  margin-left: 10rpx;
  text-decoration: underline;
}
